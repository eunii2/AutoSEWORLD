spring:
  datasource:
    url: jdbc:mysql://localhost:3307/AutoSEWORLD?useSSL=false&serverTimezone=UTC
    username: root
    password: 7562
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create  # 테이블 자동 생성/업데이트
    show-sql: true  # SQL 쿼리 로깅
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    org.hibernate.SQL: DEBUG  # SQL 쿼리 디버그 레벨로 로깅

server:
  port: 8080